// Single source of truth for the version string shown in HeaderBar.tsx and AboutModal.tsx —
// previously hardcoded independently in both places (v0.3 polish pass).
export const OSHIPFP_VERSION = 'v0.3.0'
