import Card from './Card'

export default function ExportPanel() {
  return (
    <Card title="Export" dot="dim" meta="PNG">
      <p style={{ color: 'var(--text2)' }}>Downscale presets and resample mode — coming soon.</p>
    </Card>
  )
}
