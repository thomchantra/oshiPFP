import Card from './Card'

export default function MaximizerPanel() {
  return (
    <Card title="Maximizer" dot="dim" meta="Line art">
      <p style={{ color: 'var(--text2)' }}>Threshold, grow radius, and layer stacking — coming soon.</p>
    </Card>
  )
}
