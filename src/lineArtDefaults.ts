import type { LineArtMode, LineArtParams } from './types'

/** Cosmetic display names, keyed by internal mode id — shared by LineArtPanel's algo selector and
 * App.tsx's dev-only state dump (src/debug/dumpState.ts), which both need the same human-readable
 * name without either owning the canonical list. */
export const LINE_ART_LABELS: Record<LineArtMode, string> = {
  pathB: 'Botan',
  pathC: 'Chie',
  pathD: 'Daiya',
  pathF: 'Fumiko',
  pathG: 'Gumi',
  pathH: 'Hinata',
  pathI: 'Inori',
}

/** Per-algorithm default overrides — used both by LineArtPanel (when switching modes fresh) and App.tsx (to seed each algorithm's initial cached params, see the per-mode memory there). */
export const LINE_ART_MODE_DEFAULTS: Record<LineArtMode, Partial<LineArtParams>> = {
  pathB: {
    opacity: 1,
    threshold: 0,
    radius: 1,
    hardness: 1,
    blobContrast: 1,
    colorExpansion: true,
    colorContrast: 1,
    tintColor: [0, 0, 0],
  },
  pathC: { opacity: 1, gateThreshold: 0.05, radius: 1, hardness: 1 },
  pathD: { opacity: 1, threshold: 0.05, radius: 1.5, colorMode: 'vivid', tintColor: [1, 0.475, 0.886], vividDeadzone: 0.15, vividBoost: 1 },
  pathF: {
    opacity: 1,
    sensitivity: 3,
    radius: 0.2,
    colorMode: 'findEdge',
    saturation: 0.5,
    tintColor: [1, 0.475, 0.886],
    vividDeadzone: 0.15,
    vividBoost: 1,
  },
  pathG: {
    opacity: 1,
    threshold: 0.5,
    radius: 1,
    blobContrast: 1,
    colorExpansion: false,
    colorContrast: 1,
    tintColor: [0, 0, 0],
    gumiContrastBoost: 1,
    blobMaxDt: 2,
    gumiOverdrive: 0,
    gumiLineFillType: 'solid',
    gumiLineSolidColor: [0, 0, 0],
    gumiLineInvert: false,
    gumiGapClosing: true,
    gumiBlobGamma: 1,
    gumiColorBleed: false,
    gumiBleedFeather: 1.5,
    gumiBleedRadius: 8,
    gumiSoftDetection: false,
    gumiSoftness: 0.1,
    gumiFillMode: false,
    gumiFillRadius: 8,
    gumiFillInvert: false,
    gumiFillType: 'image',
    gumiFillSolidColor: [0, 0, 0],
    gumiFillPixelThreshold: false,
    gumiGradientMap: false,
    gumiGradientShadow: [0, 0, 0],
    gumiGradientMid: [0.5, 0.5, 0.5],
    gumiGradientHighlight: [1, 1, 1],
    gumiRampFloor: 0,
    gumiRampInnerLow: 0,
    gumiRampInnerHigh: 0.6,
    gumiRampCeiling: 1,
    gumiRampFeather: 0.12,
  },
  pathH: {
    opacity: 1,
    threshold: 0.5,
    radius: 1.5,
    thresholdEnabled: false,
    hiToneTarget: 'off',
    hiToneGain: 1,
    hiToneContrast: 1,
    highPassStrength: 1,
    highPassResponsiveColor: false,
    responsiveCrossover: 0.5,
    responsiveGrow: 0,
    responsiveGrowBias: 0,
  },
  pathI: {
    opacity: 1,
    threshold: 0.5,
    thresholdEnabled: false,
    hiToneTarget: 'off',
    hiToneGain: 1,
    hiToneContrast: 1,
    laplacianStrength: 1,
    laplacianPreBlur: 0,
    laplacianSharpenAmount: 0,
    laplacianGrow: 0,
  },
}
