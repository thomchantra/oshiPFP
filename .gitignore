referencecode/
# JSON preset saga — raw staging triplets for scripts/import-presets.sh. Disposable: the script
# consumes and archives these into presetsprep/_imported/; the real shipped artifacts are
# public/presets/ (webp) + src/presets/data/ (trimmed JSON), both tracked normally.
presetsprep/

# v0.5 Instagram-mode baseline-discovery lab (lab-grid.html) test corpus —
# curated illustrations saved from the web for empirical tuning, not
# redistributable assets. Drop images here locally; never committed.
lab-test-images/

node_modules/
dist/
dist-ssr/
.env
*.local
node_modules/.tmp/
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.log

# Internal working docs
.claude/
/CLAUDE.md
/changelog/
/docs/