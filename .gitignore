referencecode/
# JSON preset saga — raw staging triplets for scripts/import-presets.sh. Disposable: the script
# consumes and archives these into presetsprep/_imported/; the real shipped artifacts are
# public/presets/ (webp) + src/presets/data/ (trimmed JSON), both tracked normally.
presetsprep/
node_modules/
dist/
dist-ssr/
.env
*.local
node_modules/.tmp/
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.log

# Internal working docs — dev-session continuity notes, not meant for the public repo. Kept on
# disk for continued local use (Claude Code still auto-loads CLAUDE.md from the project root),
# just no longer git-tracked. src/lab/README.md is NOT covered by this — it's source-adjacent
# documentation meant to ship publicly.
/CLAUDE.md
/changelog/
/docs/